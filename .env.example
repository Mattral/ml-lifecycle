# Environment Variables
# No secrets required — this is a client-side simulation app.
# All data is generated in-browser, no external APIs needed.

# If deploying behind a CDN/reverse proxy:
# VITE_API_BASE_URL=https://api.yourdomain.com

# Node environment (set by CI/CD)
# NODE_ENV=production
